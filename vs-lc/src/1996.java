public class 1996 {
	    public int numberOfWeakCharacters(int[][] properties) {
		Arrays.sort(properties,(a,b)->{
			return Integer.compare(a[0],b[0]);
		});
		int[] r_max=new int[properties.length];
		for(int )
    }	
}
